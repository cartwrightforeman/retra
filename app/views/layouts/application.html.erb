<!DOCTYPE html>
<html lang="en">
  <head>
    <script src="https://use.fontawesome.com/7de3539fba.js"></script>
    <link href="https://fonts.googleapis.com/css?family=Lato|Petrona" rel="stylesheet">
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Retra" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1 class='title'><a href="/">Retra</a></h1>
        </li>
      </ul>

      <section class="top-bar-section">
        <ul class="right">
          <% if current_user %>
            <li><%= link_to "Sign Out", "/signout" %></li>
            <img class="profile-photo" src="<%= "#{current_user.image}" %>"/>
          <% else %>
            <li><%= link_to "Sign In", "/auth/github" %></li>
          <% end %>
        </ul>
      </section>
    </nav>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
